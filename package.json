{
  "name": "intercorrencias",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass src/css",
    "sass-watch": "sass --watch src/style.scss:src/css/style.css",
    "browser-sync": "browser-sync start --server --files 'src/css/*.css'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "5.3.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
